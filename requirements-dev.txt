# Optional independent offline export verification; not needed to run the web app.
# Verified with CPython 3.11 on Windows.
cadquery-ocp==7.8.1.1
trimesh==4.8.1
numpy==1.26.4
# Trimesh's component checks require an optional graph engine on clean CI images.
networkx==3.4.2
