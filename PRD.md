# 大画幅镜头板 / 法兰生成器 · 产品需求文档

版本：0.9 · 日期：2026-09-15 · 状态：v0.2.4 螺纹导向倒角增量已自动验证、按用户授权推送并成功部署Pages；公开HTTPS九项浏览器与Linux完整CI通过。未验证的其他需求不作为已实现。完整验证见 docs/IMPLEMENTATION_STATUS.md / docs/DEPLOYMENT.md。

## 1. 产品目标

建立可由 GitHub 仓库管理、优先部署到 GitHub Pages 的免费静态网页。用户选择已有镜头板模板或参数化法兰，在指定位置添加孔及加工特征，在浏览器内预览并下载制造用模型。无需 MakerLab，无需为每次设计手动上传 `default.stl`，无需账户或服务器端 CAD 服务。

优先输出真正的实体 STEP（`.step`），同时支持 STL（`.stl`）。v0.1.0 已验证样例的实体 STEP / STL 输出，非所有参数组合的成功承诺；任何降级必须说明原因、由用户选择。界面提供简体中文 / English 切换，首次使用默认简体中文。

用户已确认：网页原创代码、自己绘制的初始镜头板 STEP 模板及基于这些模板生成的可版权模型文件，以 GNU GPL 第 3 版（`GPL-3.0-only`）发布，见 [LICENSE](LICENSE) 与 [许可范围](docs/LICENSING.md)。第三方库保留各自许可，不将所有依赖重新标记为 GPL。

## 2. 现有参考与边界

已阅读本地 `scadtest` 中的 OpenSCAD 文件，仅作功能参考，整个目录保持本地并被 `.gitignore` 排除。

初始六个自绘 STEP：Graflex Pacemaker 4×5 简化空白板；Horseman 空白板 / 简化空白板；Linhof 空白板；Sinar 空白板 / 简化空白板。v0.2.0 新增 ALPA、Arca 141、CAMBO TWR54 简化、TOYO 158 简化，目前十模板。具体文件与校验标识见 [模板目录](docs/TEMPLATE_CATALOG.md)。仅纳入 `.step` / `.stp`（大小写不敏感），目录内所有其他文件忽略，不读取其模型内容、不提交、不打包，原文件不修改。

自绘与许可已由用户确认，但文件名不等于实机适配证明；实体有效性、单位、基准、厚度和卡口保护区仍须 CAD 导入检查。模板 STEP 作为可发布源资产，运行时直接内置加载；用户自由上传 STEP 仍属 P1，与内置模板的 STEP 导入初始化不同。

| 参考 | 已有功能 | 新项目处理 |
| --- | --- | --- |
| SinarLensboard 系列 | 导入 STL、中心孔、局部减薄、圆周孔、正反面文字 | 保留功能意图，补充自由孔位、坐标定义和校验 |
| flange_generator 系列 | 两级圆环、内螺纹、圆周孔、锥形沉头、外缘扳手槽 | 参数化重建，增加螺纹模式、公差和明确沉头几何 |
| ParametricModelMaker | `default.stl` 路径导入和文字 | 内置模板自动加载，不依赖这个文件名 |
| threads.scad | 网格螺纹、隐含打印配合补偿 | 不照搬算法或默认补偿；重新定义名义尺寸与制造补偿 |

参考代码的 `139.5`、`65.4`、`M65 × 1` 等数字不是经过确认的厂家标准。`mounting_hole_pos = 45` 是孔心圆半径，对应 PCD 90 mm，不能误标为 PCD 45。旧沉头代码的 `1.414` 系数不作为新沉头角度的定义。MakerLab 版本中声明的文件名与实际导入路径不完全一致，新项目不沿用这种路径依赖。

## 3. 用户与工作流

主要用户：自行打印配件的大画幅摄影爱好者；需要交给加工方 STEP 的设计者；维护模板的贡献者。

典型流程：选择镜头板 / 法兰 → 选择内置 STEP 模板 → 确认尺寸与正反面 → 默认普通通孔，选择快门开孔预设或自定义孔径 → 按需切换 M 型内螺纹 → 添加独立孔或孔阵列 → 检查警告与预览 → 下载 STEP / STL、参数 JSON 与许可说明。

桌面为完整编辑主场景；窄屏可折叠参数面板，但不能隐藏校验和导出状态。

## 4. 范围与优先级

P0：首个可用版本的必备需求。P1：后续增强，不阻塞 P0。P2：长期候选。实现前新增的建议已在本节显式纳入，而不是隐含扩张范围。

### 4.1 P0：模板与基础实体

- 内置模板选择、缩略图、名义尺寸、来源、许可证和验证状态。选择即加载，不要求额外上传 STL。
- 产品模板使用 `steps/` 中已登记的十个自绘 STEP，完整版 / 简化版分别显示，不静默合并或更换几何。另有参数化两级圆环法兰；原创可调矩形板仅作几何测试夹具，不替代用户模板。
- 各模板入库前执行实体有效性、单位、安装中心 / 正反面、厚度及卡口保护检查。未确认装机时标记“实验 / 未验证”，不能宣称兼容全部同品牌相机；准确兼容声明需要实物测量与装机记录。
- 模板支持固定保护特征与可修改区域，不能为开孔破坏卡口、边缘定位结构或遮光结构而不提示。
- 法兰参数：外径、台阶外径、底部厚度、台阶高度、中心孔 / 内螺纹、安装孔阵列、扳手槽数量 / 宽度 / 径向深度 / 起始角。槽数为 0 表示关闭。
- 多零件方案分别导出镜头板和法兰；不把预览摆放偏移写入零件制造坐标。组合预览不代表完成机械装配验证。

### 4.2 P0：孔与局部加工

- 镜头板中央结构默认启用普通通孔，而不是螺纹孔；新设计暂定预选 Copal / Compur #0、d=34.6 mm、X/Y=0、零制造补偿。若模板基准存在专用光轴偏置，由模板元数据说明，不能因选快门而自动更改基准。
- 提供 [常见快门开孔预设](docs/SHUTTER_PRESETS.md)：Compur #00 26.3 mm；Copal / Compur #0 34.6 mm、#1 41.6 mm、#3 65.0 mm；Compur #2 52.5 mm；Copal #3s 64.1 mm；Copal Press #0 / #1 分别 34.6 / 41.6 mm。#2 和 #3s 标明型号 / 版本差异，须实测确认。
- 每个预设记录稳定 ID、适用品牌 / 型号、通孔直径、来源 URL、查阅日期和风险说明；网页展示来源入口，可自定义孔径。选预设不会悄悄增加加工余量，孔径输入被修改后显示“自定义”，JSON 保留参考预设与实际数值。
- 中央通孔可设置直径及 X/Y 偏移，不限制只能在中心；预设孔径不适合当前模板时提示，不自动缩小孔径或忽略孔。
- 任意多个独立特征：创建、选择、复制、编辑、删除、启用 / 禁用；每个特征有稳定 ID。
- 通孔：直径。盲孔：直径、深度、正面 / 背面；MVP 孔轴仅垂直于板面，盲孔为平底，不模拟钻尖。
- 锥形沉头孔（countersink）：底孔直径、沉头大径、包含角、加工面；沉头深度由这些参数推导。角度是完整夹角，而非半角。参考公式 `h = (D - d) / (2 × tan(α / 2))`，要求 `D > d`、`0 < α < 180°`。推荐展示 90°，但允许手动输入，不宣称匹配所有螺钉。
- 圆柱沉孔（counterbore）：底孔直径、沉孔直径、沉孔深度、加工面；与锥形沉头分开命名。底孔可设通孔或盲孔，组合深度需要校验。
- 可选 M 型公制内螺纹：主要输入为“螺纹公称外径 D（mm）”与“螺距 P（mm）”，显示 `M{D}×{P}`；用户所说“缠距”统一称为螺距，单线时导程等于螺距。目标为 60° M 型基本牙型，牙顶 / 牙底截形与大小径须在 M1 验证，不以尖锐三角 V 槽冒充合格基本牙型；不承诺未验证的标准公差等级或加工精度。
- 高级参数：有效长度、入口面、右旋 / 左旋（默认右旋）、单线、独立配合补偿。通孔切换到 M 螺纹时不把原通孔直径自动当作螺纹外径，要求确认 D 与 P；切回通孔可恢复此前通孔参数。公称外径是配合外螺纹的大径，不是攻丝底孔直径。
- 快门通孔预设只填通孔尺寸，不自动生成“同直径 M 螺纹”。例如 Copal #3 通孔 65.0 mm，而快门安装螺纹资料是 M62×0.75；前后镜片组螺纹也不是板上安装螺纹。螺纹参数以用户确认的实际配合件为准。
- 螺纹有两种导出模式：“真实几何螺纹”和“攻丝底孔 + 参数说明”。后者导出的是无牙底孔，底孔直径由用户确认，必须在界面、文件名和 JSON 中明确标记，不能称为已生成实体螺纹。
- 真实几何螺纹是 P0 的技术验证项；若验证失败，先调整 PRD / 发布范围并征得需求确认，不悄悄用普通圆孔替代。
- 局部减薄 / 法兰座：圆形区域直径、中心位置、加工面、目标剩余厚度；深度由当地基础厚度推导，不能混淆“剩余厚度”和“切除深度”。非均匀模板区域需计算局部厚度，不能仅依赖最大包围盒厚度。
- 圆周阵列：孔数、PCD、中心 X/Y、起始角、孔类型；支持矩形阵列的需求列入 P1。独立孔与圆周阵列可以同时存在。
- 参数输入为精确定位依据，二维俯视图点选辅助定位；点选始终显示坐标，支持回到数字编辑。

### 4.3 P0：坐标、尺寸和制造安全（新增）

- 内部统一 mm、degree；禁止把 STL 的未知单位当作可靠 mm。首版不提供界面单位切换。
- 原点：模板指定的安装中心；Z=0 为后侧基准平面，前侧为 +Z。前视图从 +Z 看向原点时 +X 向右、+Y 向上。角度从 +X 开始，前视图逆时针为正。背面视图要提示镜像视觉关系，但数值坐标不变。
- 孔位不接受 NaN、Infinity、负直径、零厚度、零螺距；允许关闭台阶 / 阵列时明确使用 0，不用负数作关闭开关。
- 校验孔位越界、孔间相交、保护区冲突、最小残余壁厚、沉头 / 沉孔穿透、盲孔超深、螺纹长度与当地厚度。边缘距离按实际模板实体 / 保护区域计算，不只检查矩形包围盒。
- 预期的底孔 + 沉头交叠属于同一个复合特征，不误报为孔间冲突；两个独立切削特征相交至少警告，形成断裂 / 零厚度 / 无效实体时阻止导出。
- 区分阻断错误（无法生成有效几何）和制造警告（可能过薄 / 不适合当前材料）。警告需用户确认后导出，阻断错误不可绕过。
- 最小壁厚警戒值可配置，演示默认 1.5 mm，仅是提醒阈值，不是材料强度保证。必须显示零件未经承重认证、重镜头需独立支撑。
- 制造补偿默认 0：孔径增加量定义为直径增量；螺纹径向间隙定义为径向量，与公称直径和牙型几何分开。界面同时显示名义值与实际值，不能复用旧库隐含的百分比补偿。
- 中央普通通孔提供“3D 打印优化”复选框，默认不勾选。勾选时应用正的孔径增加量，默认 0.5 mm，可调整但必须 >0；实际孔径=名义孔径+增加量。增加的是直径，不是半径，例如 #0 34.6→35.1 mm、#3 65.0→65.5 mm。未勾选时保存用户的增加量但实际补偿为 0，不改写快门标准值，不与另一孔径补偿重复相加。
- 优化用于镜头板中央普通通孔（含自定义名义孔径），不自动改变安装螺钉孔、沉头大径、M 螺纹外径或螺距；M 模式中保留但不应用该选项。实际增大后的孔参与保护区 / 壁厚校验、预览与 STEP/STL 导出；JSON 保存 enabled 和 diameterAllowanceMm。
- 提供小型配合测试环 / 测试片参数预设（共享同一螺纹与孔径补偿），供打印前试配；它不能替代实物验证。切削加工默认不施加打印补偿。

### 4.4 P0：预览、导出与项目保存（新增）

- 三维旋转 / 缩放、前 / 后 / 侧视、复位视角，叠加坐标轴与基本尺寸；二维视图显示孔中心、孔径及 PCD。当前选中特征高亮。
- 轻量预览允许暂时简化牙型，但界面明确“螺纹预览简化”，最终导出必须使用用户选定的真实几何或底孔模式。
- 重计算在 Web Worker 内；参数快速改变只展示最新版本。模型过期 / 计算中 / 失败时不允许导出上一次模型冒充当前模型。
- B-rep 模板及新建法兰优先实体 STEP，同时生成 STL。STEP 不附带原始应用特征树；可重新打开设计依赖项目 JSON，而非 STEP 参数反求。
- STL 提供预览 / 标准 / 精细网格档位，明确误差和单位说明；文件必须是封闭、可制造的有效网格。STL 本身不可靠保存单位，附带说明 mm。
- STEP 写出失败时仅在当前有效几何仍可网格化的情况下提供明确的 STL 替代下载；CAD 内核整体加载失败或布尔运算失败不能声称自动回退可用。
- JSON 保存 / 导入：包含 schemaVersion、模板 ID / 版本、单位、特征、公差、螺纹模式；几何参数不依赖中文或英文标签。未知版本或损坏文件不能覆盖当前设计。
- 保存语言和最近设计到浏览器本地存储；未授权模型不上传。内置模板可恢复，用户上传模型不塞入 localStorage；容量不足时提示并允许下载 JSON。语言记忆与设计保存相互独立。
- 撤销 / 重做、恢复模板默认值；重置 / 导入覆盖当前项目要确认。
- 文件名包含零件类型、模板、关键尺寸、螺纹模式与配置短标识；另附参数清单 JSON，记录生成器 / 模板版本。标识基于参数，不含私人路径或用户身份。
- 导出提供同名许可 / 来源说明与 GPL v3 文本下载；项目 JSON 保存 `license=GPL-3.0-only`、模板许可 / 来源及可重建参数。下载单一几何文件不自动上传到 GitHub。基于已许可模板的衍生输出沿用其许可，不声称仅因生成器采用 GPL 就能给任意用户资产强制换许可。
- 第一版 STEP / STL 使用 ZIP 一次性交付几何、项目 JSON、对应原始 STEP（镜头板模式）、许可 / 来源说明、GPL v3 全文；另可单独下载参数 JSON。这样不会因多次浏览器下载被阻止而遗漏来源与重建信息。

### 4.5 P0：语言与可用性

- 提供亮色 / 暗色主题切换；首次固定暗色，保留已有手动选择。参考用户 [MutexTechBellowGeneratorV2 的样式](https://rayforloy.github.io/MutexTechBellowGeneratorV2/styles.css) 的深蓝 #06121f / #0a1b2d、青色 #2ee8ff、浅蓝文字；不复制其脚本或资产。主题不进入项目 JSON、几何 revision 或 CAD 重建。
- 顶栏提供 Report 按钮，指向已确认的项目 https://github.com/RayforLoy/LensboardGenerator/issues，在新标签页打开并显示中英文无障碍说明。点击不会创建 Issue、自动附带模型 / JSON / 私人信息，也不需要网页内 GitHub 登录或令牌。
- 上轮已按用户授权发布 v0.1.0；0.2.0–0.2.3 当时仅本地迭代，未独立发布。用户本轮授权 0.2.4 验证后提交推送，包含上述增量；仍须排除 scadtest / 非 STEP 文件，以真实远端测试確認新增十模板及凸凹/倒角功能，不以 localhost 代替远端验证。
- 首次访问固定 `zh-CN`，不由浏览器英文语言自动覆盖；用户选择后保留 `en` / `zh-CN`。
- 切换语言不改变参数、坐标、模板或当前模型，也不触发无意义的重建。
- 所有操作、校验、提示、状态和无障碍标签都来自语言资源；模板品牌、规格代码和文件扩展名保持不变。
- 两套语言键一致；缺失键在测试中失败，运行时使用可理解的后备文本而不是裸 key。
- 数字采用统一十进制点号解析，明确输入规则；UI 可翻译，JSON 数字不本地化。
- 键盘可操作表单和特征列表，错误与对应字段关联，不能只通过红 / 绿区分状态。

### 4.6 P1 / P2：有意延后

P1：用户 STEP 模板导入与平面基准选择；旧 STL 导入后布尔加工及 STL 导出（需独立验证可靠的网格路线）；更多经过验证的相机模板；矩形阵列；刻字 / 浮雕及字体授权；外螺纹配对锁环；详细剖面测量；参数链接分享；离线缓存。

P2：DXF / 二维加工图、3MF、材料强度估算、完整标准螺纹库、倾斜孔、多轴加工、自动 STL 逆向、社区模板目录。

首版不包含：登录、云项目库、付费服务、服务器文件上传、任意脚本执行、自动生成 CNC 刀路、保证打印后的尺寸或承重、自动宣称厂家兼容。

## 5. 技术方向与 STEP 决策

### v0.2.4 螺纹入口导向倒角（编码前登记）

- 新设计的 M 螺纹默认启用导向倒角，默认自动跟随螺距，尺寸 C=1.2×P mm；可关闭或切换为手动尺寸。固定 45°，C 同时是轴向深度和径向增量（不是直径增量）。入口为最终坐标的正面 +Z，支持模板翻转、凸凹端面和偏心孔。
- 从真实螺纹的小径圆柱边缘生成锥形入口；真实模式入口半径=小径/2+径向间隙+C，保护包络取入口半径与公称大径半径的较大者。攻丝底孔模式从用户确认的底孔边缘倒角，入口半径=底孔半径+C，仍明确为无牙底孔。不把 C 加到公称外径或螺距，不应用普通通孔的打印补偿。
- 螺纹加工长度不因倒角自动增加；倒角会占用入口牙型，显示倒角后完整牙型长度约为 length−C，提醒试配和承重限制。真实模式要求 C<螺纹长度；两模式均要求 C<当地材料厚度，拒绝零/负/非有限/超过 30 mm 的开启尺寸及穿壁、孔间/附件冲突和无效实体。
- schema 4 保存 thread.chamfer={enabled,mode: pitch|custom,sizeMm}，自动模式实际尺寸从螺距推导，手动值独立保留。严格迁移完整 schema 1/2/3 为倒角关闭，避免旧 JSON/本地设计的几何被静默改变；拒绝残缺、额外字段及未知模式。支持双语、撤销重做、保存、JSON/ZIP来源说明和 STEP/STL。
- 验收：自动跟随、手动/关闭、无效尺寸和当地超深、旧三版本迁移；左右旋/径向间隙、不同螺距、翻转凸凹和攻丝底孔有效实体。独立 STEP 分类测量三处 45°锥面及入口直径、保留真实牙型与旋向；STL闭合、体积/尺寸一致，底孔倒角切除体积与解析公式比较；浏览器参数往返、下载和双语。版本更新为 0.2.4，更新 changelog，白名单提交既有本地增量与十模板并推送，检查 Actions/Pages，不发布私有参考或 Downloads 参数。

### v0.2.3 凸凹端面孔保护区修复（编码前登记）

- 加工前读取实际当地表面；孔心在新端面内腔内，且当地材料上下表面均处于新端面 Z 厚度区间时，使用凸凹端面内腔轮廓及 1.5 mm 孔余量校验，不套用原模板 editableRadius。剩余原板上的孔继续使用原模板保护圆区；不能仅因启用凸凹就全局豁免。
- 中央普通孔、真实 M 螺纹 / 攻丝底孔、局部减薄、独立孔 / 圆周孔共享规则；减薄后的端面仍属于端面。保留当地材料 / 厚度、附件保护、孔间冲突、端面穿壁及无效实体检查。不恢复用户已取消的基座材料校验。
- 将用户 ALPA、翻面、80×80 / R8、直壁、+17 mm、端面 2.5 mm 的 Ø60 / M65×1 参数重建为原创代码测试样例，不复制 Downloads 原 JSON 或私人路径到仓库。验证两孔成功及真实牙型、同尺寸平板 / 原板孔仍阻止、独立端面孔和局部减薄、穿壁 / 超长螺纹拒绝，独立 STEP / STL 重读及浏览器 JSON 导入 / 下载。
- 版本 0.2.3，schema 3 保持不变，更新日志及验证状态；本轮不自动提交 / 发布。

### v0.2.2 用户指定调整（编码前登记，取代下述旧版基座保护规则）

- 禁用整个基座覆盖区的材料完整 / 厚度均匀校验及其外扩 1.5 mm 余量。基座尺寸只核对翻转后主镜头板的 XY 包围范围，不包含附件；圆角矩形按外长 / 外宽、圆按直径，中心仍是光轴原点。等于范围允许，超出任一侧时报错，不比较面积，不检查圆角 / 凹槽投影。
- 跨台阶 / 背筋不再因该校验拒绝；原板内腔切除穿过原主板完整 Z 范围，避免厚度变化处残留封闭内膜。显示制造警告：基座可能切除原筋位 / 安装或遮光结构，尺寸检查不代表结构安全，需人工检查。
- 保留尺寸 / 角度参数有效性、孔 / 端面内腔限制、真实三维附件干涉及最终有效实体检查；用户取消的是基座材料校验，不是允许无效 CAD 输出。当地中心表面仅用作凸凹间距基准。
- 版本 0.2.2，JSON 结构仍为 schema 3；更新双语报错、需求及 changelog。验收圆 / 矩形超尺寸与刚好相等、非对称主板范围、Linhof / CAMBO 默认 60 mm 跨厚度变化基座、翻面 / 凸凹导出独立重读。旧规则及历史验证保留为历史记录，不再作为当前阻断规则。

### v0.2.1 增量范围（编码前登记）

- 修复凸凹基座误用通孔圆形保护区：按含 1.5 mm 安全余量的实际圆 / 圆角矩形轮廓检查材料完整、当地厚度一致与真实三维附件干涉；真正进入边缘、筋位或附件仍阻止，不自动缩小用户尺寸。
- 模板选择处提供独立前后翻转（绕 Y 轴 180°，以主板 Z 范围中点为旋转基准）及上下翻转（绕 Z 轴 180°）。采用刚体旋转而非镜像；主板与所有附件一起旋转，保留原始 STEP 字节、光轴 XY 原点和主板 Z 基准。切换模板恢复未翻转。
- 先翻转源模板，再按最终坐标加工；孔位、凸凹间距及螺纹旋向不被偷偷镜像。正面始终 +Z；翻转同步 STEP / STL、JSON、撤销和本地保存，不只是相机视角。
- 项目 schema 3 保存 orientation.frontBack / upDown；严格迁移 schema 1 / 2 为未翻转，保留旧参数，拒绝残缺或未知字段。
- 预览显示真实世界原点的 X 红 / Y 绿 / Z 蓝坐标轴、箭头和标识，随视角投影，+Z 为加工正面；坐标轴不触发 CAD 重建，资源随预览销毁。
- 验收覆盖原误报矩形、小模板真实边界、附件前后翻转、四种旋转组合、偏心加工坐标、旧 JSON 迁移、双语与独立 STEP / STL 重读。相机装配、承重、漏光和实物试配仍未验证。

### v0.2.0 增量范围（编码前登记）

- 镜头板可启用凸 / 凹板；默认关闭保持旧平板。基座类型默认圆角矩形，长 × 宽（初始演示 60 × 60 mm）、圆角半径（8 mm），或圆形外径（60 mm）；尺寸均指原板正面上的基座外轮廓。基座中心固定模板原点，中央孔仍可偏心，不随孔位移动基座。
- 壁厚默认 2 mm，朝内生成，定义为斜壁法向厚度，水平内缩量 wall / sin(angle)。端面厚默认 2.5 mm，是 Z 向厚度。倾角默认 80°，相对原板平面，90° 为垂直；支持 45–90°。
- 间距默认 +17 mm，定义为原板当地正面到新端面正面的有符号 Z 距离；正为凸、负为凹，零拒绝（平板请关闭功能），绝对值上限 100 mm。两种方向的壁均朝端面收窄，横向收缩 abs(spacing) / tan(angle)，圆角作内偏置并可退化到 0。正间距必须大于端面厚，负间距端面背侧在 spacing−faceThickness。
- 实际切开原板内轮廓、融合中空斜壁与端面，保留外部安装结构 / 附属实体；不叠加实心台块或仅移动完整模板。中央孔 / M 螺纹 / 局部减薄按新端面当地材料加工，其他孔可加工剩余原板；跨壁、薄端面、越界及破坏保护区均拒绝或明确警告。
- 保护检查包括基座外轮廓及 1.5 mm 余量的当地完整均匀材料、模板保守区域和附件；不只凭包围盒融合。过小端面 / 内腔、无效或碎裂实体、法兰模式误启用都阻断。凹板需人工检查机身空间、快门操作、遮光与打印支撑，当前不自动验证。
- 项目 schemaVersion=2；严格验证旧 schema 1 后增加关闭的凸凹板参数并更新生成器版本，未知结构不得覆盖当前设计。新参数进入 JSON、revision、ZIP 命名 / 来源，撤销与重做保持完整。
- 新增 ALPA / Arca 141 / CAMBO TWR54 / TOYO 158 用户自绘 STEP，总计十模板，GPL-3.0-only，登记原字节 SHA-256 / 基准 / 当地厚度 / 保守区，继续忽略其他扩展名。无实物认证仍标 experimental。
- 三维预览使用更清晰的浅色非高金属材质、深蓝背景及方向光，默认显示 B-rep 的真实拓扑边而非所有三角网格线；可关闭。边线是视图偏好，不触发实体重建，不写项目 JSON。未来 STL 模板默认不显示边线，本轮不添加 STL 导入。
- 升级应用 / 包 / 导出版本到 0.2.0，增加 CHANGELOG；进行凸凹 × 圆 / 圆角矩形 × 80 / 90 度实体 / STEP / STL 回归、端面孔与螺纹、新模板逐个浏览器加载、旧 JSON 迁移、默认暗色及边线开关验证。

增量验收 A20：凸凹尺寸、内壁 / 端面、真实中空、连通实体及独立重读；A21：默认暗色、匹配参考配色、边线可切换且不重算；A22：十个源 STEP 字节不变、模板初始化与导出、schema 1→2、版本 / changelog 一致。公开部署仅在用户再次要求时执行。

暂定 React + TypeScript + Vite，Three.js 预览，Replicad + OpenCascade.js / WASM 在单个 Web Worker 中建模，GitHub Actions 构建静态文件并发布 Pages。具体版本和兼容组合在技术验证后锁定，不在需求阶段编造依赖版本。

GitHub Pages 是静态托管，不能在 Pages 内运行 Python / CadQuery / FastAPI 服务。浏览器 CAD 路线保留离线几何计算和无账户体验。Python `requirements.txt` 此时只做用途说明，不是网页依赖入口。

Replicad 官方 Shape API 提供 `blobSTEP()` 与 `blobSTL()`，因此 STEP 具有可信的技术入口，但项目本身仍需验证布尔操作、真实螺纹、输出可读性、WASM 内存和子路径部署。使用普通单线程 WASM Worker，不将依赖 SharedArrayBuffer / 跨源隔离的多线程构建作为首版前提。

STL 是三角网格，不能直接恢复解析圆柱、螺纹和原始 CAD 结构。将每个三角面包装进 STEP 不算达到本项目“精确可加工 STEP”的目标。推荐对内置模板从可靠尺寸 / STEP / 原创草图重建 B-rep；保留 STL 仅作后续网格路线，不伪装转换能力。

## 6. 性能与资源目标（待验证）

首版资源上限暂定：每个零件 100 个切削特征、单线螺纹最多 40 圈；超过时阻断并说明限制。后续根据基准调整，不因 UI 卡顿悄悄丢弃特征。

基准机由后续记录实际 CPU、内存、浏览器及版本：目标是 16 GB RAM 的普通桌面设备。WASM 已加载后，20 个无螺纹孔的标准模型重建目标 ≤3 秒；20 圈真实螺纹最终导出目标 ≤30 秒。60 秒超时后终止并重建 Worker，可保留参数重试；这些是验收目标，不是当前性能结果。

显示初始化 / 计算 / 导出阶段与重试入口，不展示虚构百分比。WASM 懒加载，资产由本项目同源提供，首次加载失败有可读提示。记录下载体积和峰值内存；不把整个 OpenCascade 构建盲目塞进首页。

## 7. 验收标准

| ID | 必须得到的证据 |
| --- | --- |
| A01 | 从 Pages 项目子路径直接加载，6 个内置 STEP 模板可选，无需 `default.stl`，无后台 API；单位和基准已检查 |
| A02 | 首次为中文；英文切换 / 刷新记忆；参数与模型不变；两语言键检查通过 |
| A03 | 通孔、盲孔、两类沉孔、局部减薄、独立偏心孔、圆周阵列分别有几何回归样例 |
| A04 | PCD 90、4 孔、起始角 0°时坐标为 `(45,0)`、`(0,45)`、`(-45,0)`、`(0,-45)` mm；偏移和背面显示正确 |
| A05 | 90°沉头 `d=3.2, D=6.2` 的推导深度为 1.5 mm；当地厚度不足时正确处理 |
| A06 | 法兰台阶、中心内螺纹、安装孔、扳手槽生成有效实体；左右旋样例导出方向正确 |
| A07 | 真实牙型模式导出实际螺旋几何；底孔模式准确标记且不冒充实体螺纹；使用试配件人工检查 |
| A08 | 无效参数、越界、保护区、断裂、超深、重复异步结果有测试；失败后无过期模型下载 |
| A09 | STEP 在独立 CAD（如 FreeCAD）重新打开、是有效实体，体积 / 包围盒与源实体误差分别 ≤0.1% / 0.01 mm；真实螺纹必须保留。不是只检查文件扩展名 |
| A10 | STL 封闭、流形、法向一致，无意外独立碎片；标准档尺寸误差 ≤0.05 mm；单体和多零件分别验证 |
| A11 | JSON 导出 / 重载模型参数一致；未知版本、损坏内容、缺失模板不会破坏当前项目 |
| A12 | 最新稳定 Chromium / Firefox 桌面版本记录实测；Safari 桌面导出作为发布前兼容检查，失败需明确支持矩阵 |
| A13 | Pages 部署实测 Worker / WASM 路径、正确响应与初始化；源代码及构建物不含 `scadtest` 或未授权素材 |
| A14 | 所有宣称已验证兼容的相机模板有可追溯尺寸记录、许可与实物装机结果；实验模板显著标注 |
| A15 | 镜头板默认普通 #0 通孔 34.6 mm；快门预设有来源 / 风险提示、自定义支持；切换 M 螺纹要求 D/P 确认，不把通孔径当公称外径 |
| A16 | 初始模板及项目原创内容标记 GPL-3.0-only；导出附许可 / 来源 / 参数；第三方许可保留；steps 中非 STEP/STP 文件未追踪且未进入构建 |
| A17 | 3D 打印优化默认关闭；开启默认直径 +0.5 mm，#0 实际 35.1、#3 实际 65.5；切换预设不累计增加，关闭恢复名义值，M 螺纹不受影响，JSON 往返与实际模型一致 |
| A18 | 深浅主题切换 / 刷新保留、两语言按钮说明、暗色表单 / 图表 / 错误可读；切换不改变参数或触发 CAD 重建，窄屏不溢出 |
| A19 | Report 指向本项目 Issues，新标签页安全打开，无自动数据提交；Pages 使用 Actions 发布，公开 HTTPS 实测 CAD 初始化和 STEP / STL 下载，源码提交不含私有目录或非 STEP 原文件 |

首版发布不能仅靠截图或 npm 构建成功验收。实体有效性、尺寸、导出重读及试配检查必须有独立证据。

## 8. 实施里程碑

1. M0（已完成）：确认 6 个自绘初始 STEP 和 GPL v3；补齐快门通孔预设、M 内螺纹输入与打印补偿。
2. M1：小范围技术验证：6 个用户 STEP 导入 / 单位 / 基准 / 有效性检查 + 默认通孔 + 偏心孔 + 沉头 + 两级法兰 + M 型基本牙型真实内螺纹 → STEP/STL → 独立重读 → Pages 子路径试部署；锁定技术和依赖。
3. M2（部分完成）：v0.1.0 已实现主要编辑流程、双语、校验、JSON 和异步预览；未完成的 P0 范围见 [实施状态](docs/IMPLEMENTATION_STATUS.md)，不宣称完整发布验收。
4. M3：导出几何回归、浏览器测试、许可证清单、试配及制造提示，达到已确认的发布范围后发布。
5. M4：按需求优先补充有授权的模板和导入路线，不默认增加云服务。

## 9. 欠缺与需要用户补充的信息

| 项目 | 缺失内容 | 暂定处理 / 影响 |
| --- | --- | --- |
| 相机模板 | 已完成六模板 CAD 单位 / 变换 / 有效性与当地厚度检查；尚缺精确保护区和实机配合记录 | 保守加工区与材料探针，不发布未经证实的准确兼容声明 |
| 素材权利 | 用户已确认 steps 的自绘 STEP 采用 GPL v3；原 STL 许可不变 | 初始 STEP 可纳入；scadtest 与 steps 中非 STEP 文件保持排除 |
| 螺纹规格 | 实际镜头 / 快门公称径、螺距、牙型、旋向、有效长度 | 先接受手动参数；不将某个 Copal 编号自动当作唯一规格 |
| 制造方式 | FDM / 树脂 / CNC、材料、喷嘴、期望配合 | 默认零补偿，提醒试配；不承诺通用打印公差 |
| 结构要求 | 镜头质量、法兰固定方式、遮光、卡口配合 | 增加保护区 / 壁厚提醒；承重与漏光必须实物检查 |
| 项目许可 | 已确定 GPL 第 3 版（GPL-3.0-only），正式 LICENSE 已添加 | 第三方依赖兼容与对应源文件交付方式须发布前复核，见许可文档 |
| 发布环境 | 已确认 RayforLoy/LensboardGenerator，用户已要求 Pages | 提交并推送至已有仓库，启用 Actions 静态发布，实际结果另记；不创建其他仓库 |

模板来源与项目许可已确认，不再列为等待用户选择的阻塞项。剩余缺失不阻止 STEP 技术验证，但会限制实机兼容、标准公差和“可直接制造兼容零件”的承诺。后续开始实现前要保留“已确认 / 暂定 / 待验证”的区别。

## 10. 依据与参考

以下官方技术资料于 2026-09-14 查阅；路线结论是基于资料的设计建议，并非项目实测报告。

- [GitHub Pages 静态托管说明](https://docs.github.com/en/pages/getting-started-with-github-pages/what-is-github-pages)
- [Replicad Shape：STEP / STL 导出 API](https://replicad.xyz/docs/api/classes/Shape/)
- [Replicad 嵌入应用与 Worker / WASM 初始化](https://github.com/sgenoud/replicad/blob/main/packages/replicad-docs/docs/use-as-a-library.md)
- [OpenCascade.js 多线程与跨源隔离要求](https://ocjs.org/docs/advanced/multi-threading/intro)
- [Replicad 项目与许可](https://github.com/sgenoud/replicad)
- [OpenCascade.js 项目与许可说明](https://github.com/donalffons/opencascade.js)
- [S.K. Grimes：快门尺寸与安装螺纹](https://skgrimes.com/shutters/)
- [S.K. Grimes：镜头板安装与加工孔径差异](https://skgrimes.com/lens-board-mountings/)
- [GNU GPL 第 3 版正式条款](https://www.gnu.org/licenses/gpl-3.0.en.html)
