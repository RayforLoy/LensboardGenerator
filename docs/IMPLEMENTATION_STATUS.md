# 实施与验证状态

## v0.3.0 — 自定义镜头板与十二模板，2026-09-19

本地开发版本，未提交、推送或发布；公开 Pages 仍为 v0.2.4。本轮用户未授权自动发布。

- 新增 template/custom 镜头板来源。自定义板支持 0 圆角矩形、一般圆角、半短边跑道和等边圆形，背面 Z=0、板体 +Z；外圈等距遮光环与内圈居中圆角矩形遮光台向 −Z 增材生成。两结构完成融合后再做普通孔、复合孔、减薄或真实螺纹。
- 外圈按真实等距内轮廓生成；内外圈及凸凹板使用完整圆角矩形支持函数按 relief≤内圈≤外圈内边界≤外形逐级包含，关闭层跳过、等轮廓允许，能识别圆形边界处“长宽未越界但圆角越界”的情况。自定义凸/凹板完成开腔、壁和端面后再加工孔；遮光、承重、机身空间和打印方向仍未实物认证。
- schema 5 保存 boardSource/customBoard，严格迁移完整 schema 1/2/3/4：旧项目保持模板模式、旧倒角关闭规则和原几何。自定义 ZIP 不附带闲置模板，增加 CUSTOM-BOARD.txt；文件名、参数、来源和双语界面一致。
- 新增 Graflex Pre-Anniversary 4×5 与 Linhof Technika III/IV 6×9，十二模板原字节 SHA-256 注册。两者均为单一有效实体，绕 X +90°且无需 X 平移；标准化尺寸分别 102×100.5×6.5 mm、74.75×82×2 mm，中央当地厚度 6.5 mm、2 mm。默认 #0 开孔和独立重读通过，仍标 experimental。
- 30 项单元测试、严格 TypeScript和生产构建通过；dist 恰含十二个登记 STEP。10 项 Edge / Playwright 全部通过，覆盖自定义跑道、双遮光结构、凸凹叠加、非法层级、双语、schema 迁移、无关源模板排除、十二模板逐一加载及原功能回归；截图已人工检查。
- 116 组 CAD STEP/STL 生成及 native OCCT/trimesh 独立重读全部通过（原99组随两个模板/方向扩展，加四类自定义外形、遮光组合、自定义 M40×1、双遮光凸/凹板、跳层凸板及 relief=内圈等轮廓）：有效实体、体积、包围、材料点、实体数、STL闭合/法向/分件及左右旋牙型均通过。证据位于忽略的 tmp/cad-check 和 test-results，可由脚本重建。

尚未进行真实 Pages / Linux CI、实物装机、漏光、承重、打印试配或标准螺纹公差验证。约847 kB JS / 23 MB WASM及既有构建警告仍存在。

## v0.2.4 — 螺纹导向倒角，2026-09-15

已按用户授权推送main并成功部署Pages，合并未独立发布的0.2.0–0.2.3与十模板。公开HTTPS九项Edge浏览器全部通过，应用/对应源码匹配f048f7b；Linux完整检查全部成功（26单元、构建、99组CAD/独立重读、九项Chromium），见部署记录。以下旧版本“未推送”描述为当时的历史状态。

- 新设计默认启用固定45°入口倒角，自动C=1.2P，支持独立手动值/关闭；从基本小径或确认的攻丝底孔边缘扩张，入口在最终+Z。保护/碰撞/二维包络包含入口，底孔仍保持原小径；螺纹切除量在倒角前检查，不能以倒角冒充牙型。
- schema4严格迁移完整1/2/3为倒角关闭，保留旧加工/方向/凸凹参数；双语、实际C/完整牙型约length−C、撤销重做、存储、参数往返、两格式ZIP/C45文件名/入口说明。拒绝无效C、当地穿透、耗尽完整牙型及端面穿壁；完整牙型不足2圈提示，非强度认证。
- 26单元测试、严格TypeScript与生产/LensboardGenerator/构建通过；最终稳定产物九项Edge/Playwright全部通过（约1.7分钟），含自动跟随/手动保留/关闭/撤销/双语/重载/旧schema3迁移、两格式倒角下载及原功能回归，截图已检查。
- 99组CAD导出及独立native OCCT/trimesh重读全部通过（原91+倒角8组）：左右旋/0.2mm间隙、0.75/2mm螺距与偏心、ALPA翻面凸凹、手动C0.5和无牙底孔。STEP有效实体/体积/尺寸/实体数、STL闭合/法向/分件/尺寸通过；独立三层锥面内外分类验证45°入口、倒角以下真实牙型及左右旋，底孔主体未扩大。底孔倒角切除体积与π(rC²+C³/3)一致；C当地超深/真实牙型耗尽/入口穿壁拒绝。报告在忽略的tmp/cad-check，可用脚本重建。首轮新增ALPA测试遗漏模板参数已修正后全量重跑，未跳过样例。
- 十个源/打包/暂存STEP的SHA-256一致，白名单无私有scadtest、非STEP原文件或Downloads参数，git diff检查通过。约837kB JS/23MB WASM及既有node:module外置/大包警告仍存在。

未验证实物打印/螺纹试配、承重、遮光或制造公差等级；Firefox/Safari未测。基座仍按用户指定仅检查主板尺寸，不恢复材料/边缘余量阻断。

## v0.2.3 — 凸凹端面孔保护区修复，2026-09-15

本地开发版本，未提交 / 推送 / 发布，线上仍为 v0.1.0。schema 3 不变；v0.2.2 用户指定基座尺寸简化保留。

- 孔加工前依据当地上下表面位于新端面完整 Z 区间、孔心在新内腔中识别端面；端面孔按内腔及 1.5 mm 孔余量检查，不套用原板 editableRadius。座减薄后仍能识别；实际原板孔和板外点继续原模板保护。附件、当地材料、孔间、穿壁和有效实体检查保留。
- 原用户两份 ALPA JSON 经严格解析、未放宽模板保护半径（仍 30 mm），均直接生成有效单体；上下表面 17.5 / 20 mm，厚 2.5 mm。原 JSON 不修改，也不复制 Downloads 路径或内容到仓库；公开回归样例只重建制造参数。
- 22 单元测试、最终 TypeScript 严格检查 / 生产 /LensboardGenerator/ 构建通过；稳定产物下八项 Edge / Playwright 全部通过（约 1 分钟），包含两设计 JSON 导入 / STEP ZIP 下载、关闭凸凹恢复原板保护。初轮因先探测当地材料而改变板外点错误优先级造成两项旧提示断言失败，已保留原保护区错误并重跑全部通过，不取消原板保护。
- 91 组 CAD 导出及独立 native OCCT / trimesh 重读通过：原 87 + ALPA Ø60、真实 M65×1、X32 端面盲孔、Ø60 座减薄四组。STEP 有效 / 体积 / 包围盒 / 实体数、STL 闭合 / 法向 / 分件 / 尺寸、真实牙型与左右旋、盲孔底厚 / 座深 / 内腔 / 原外板材料分类验证通过。平板大孔、实际原板 X41 孔、X36 穿壁余量、3 mm 超长螺纹拒绝。证据在忽略的 tmp/cad-check/report.json / independent-report.json，脚本可重建。
- 十 STEP 原 SHA-256 不变、生产资产白名单通过、git diff --check 通过；无私人 Downloads / scadtest / SLDPRT / STL 源资产发布。

未验证实机配合、实际打印 / 螺纹公差、承重及遮光；未进行新版本远端 CI / HTTPS 发布验证。约 832 kB JS / 23 MB WASM 和既有浏览器外置 / 大包构建警告仍存在。

## v0.2.2 — 用户指定尺寸校验简化，2026-09-15

本地开发版本，未提交 / 推送 / 发布，线上仍为 v0.1.0。

- 已删除基座覆盖区材料完整 / 厚度一致性探针及边缘外扩 1.5 mm 余量；仅比较光轴中心基座外长 / 外宽或圆径与翻转后主板 XY 范围，各侧相等允许，超过任一侧阻止，不比较面积或把附件计入板尺寸。
- 内腔切除穿过原主板完整 Z 范围，跨背筋 / 台阶不会保留未切开的内膜。新增双语警告：可能切除安装 / 遮光结构、原筋位，外尺寸通过不代表结构或实机安全。附件真实三维干涉、孔 / 端面限制和有效实体检查仍保留。
- Linhof / CAMBO 默认 60×60 / R8、80°、#0 孔，凸凹 × 翻面八组合均生成成功。Horseman 部分未翻面的尺寸仍可能因最终无效实体或附件干涉失败，不以取消基座校验掩盖布尔失败；翻面后部分同尺寸可行，完整附件保留。
- 22 单元测试、TypeScript 严格检查与生产 /LensboardGenerator/ 构建通过；七项 Edge / Playwright 全部通过（54.1 s），包含非均匀模板默认基座、矩形 / 圆形超尺寸阻止、恢复编辑及双语制造提示。
- 87 组 CAD 导出回归及独立 native OCCT / trimesh 重读全部通过（原 79 + 非均匀默认基座八组）。STEP 有效性 / 体积 / 包围盒 / 实体数、STL 闭合 / 法向 / 分件 / 尺寸、左右旋螺纹及内腔 / 壁 / 端面分类通过；新增八组以原 STEP 原坐标逆变换独立比较基座外部材料保留，并在原板完整厚度采样内膜为空，避免用均匀厚度假设验证非均匀源。证据保存在忽略的 tmp/cad-check/report.json / independent-report.json，脚本可重建。
- 十个原 STEP SHA-256 不变，生产 dist 十 STEP 白名单无 scadtest / SLDPRT / 私有 STL，git diff --check 通过。JSON 仍 schema 3，应用 / 包 / 输出版本 0.2.2。

未做实机装配、漏光、承重、打印 / 螺纹试配及新版本远端 CI / HTTPS 发布验证；约 832 kB JS、23 MB WASM、node:module 外置与大包构建警告仍存在。以下 0.2.1 / 0.2.0 材料校验及拒绝案例是历史状态，当前基座规则以上述为准。

## v0.2.1 — 本地增量，2026-09-15

未提交 / 推送 / 发布；线上仍为 v0.1.0。原 v0.2.0 本地功能全部保留。

- 凸凹基座按实际外偏置轮廓核对完整板体材料、上下表面、实体数和三维附件包络，不再沿用通孔圆形保护半径。真正边缘 / 筋位 / 附件冲突仍阻止，分别显示可理解的双语提示。
- ALPA / Graflex 默认 60×60 / R8 已通过几何检查；当前默认孔和 +17 mm / 80° 下 Linhof 56×56、CAMBO 52×52 通过，60×60 覆盖非均匀结构。Horseman 的一些基座尺寸仍被真实厚度变化阻止；不会静默减尺寸或删结构。所有例子只是 CAD 材料检查，不代表实机兼容。
- 模板前后 / 上下翻转，主板基准、所有附件与原光轴原点保留；先旋转源模型再按最终坐标加工，方向进入 schema 3、Worker revision、STEP / STL、撤销 / 保存。schema 1 / 2 严格迁移；切换模板清除方向。
- 真实世界原点的 XYZ 箭头及标识，Z 是加工正面；相机操作与坐标轴不重建 CAD，箭头几何 / 标签纹理 / 材质释放。
- 21 单元测试和 TypeScript 严格构建通过，生产子路径构建通过。既有约 832 kB 主 JS / 23 MB WASM 和 node:module 浏览器外置警告仍存在。
- 最终 79 组 CAD 导出与 native OCCT / trimesh 独立重读全部通过：原四十组、十模板三种非默认方向三十组、四模板实际轮廓 / 翻面凸板八组、双体附件翻面凸板一组。STEP 有效 / 实体数 / 体积 / 包围盒、STL 闭合 / 法向 / 分件 / 尺寸，以及最终坐标偏心孔、壁 / 内腔 / 端面分类与背侧附件保留均通过；源模板基准数据用于方向尺寸断言。两方向真实附件冲突及过大基座阻止。证据在忽略的 tmp/cad-check/report.json / independent-report.json，脚本可重建。
- 稳定生产子路径 /LensboardGenerator/ 下六项 Edge / Playwright 全部通过（47.6 s）；新用例覆盖方向 JSON / STEP ZIP、重载、撤销 / 重做、双语、模板选择重置与真实坐标轴截图。已人工查看翻转凸板截图，XYZ 原点箭头 / 标签清晰。初轮按钮图标定位错误及构建期间资源替换导致的测试失败均在稳定产物上重跑通过；测试报告可变引用已修正为快照，全部导出重新生成验证，不手改证据。
- 十个原 STEP SHA-256 全部一致，生产 dist 十个 STEP 白名单，不含 scadtest / SLDPRT / 私有 STL，git diff --check 通过；本轮无远端 Linux CI 或公开 HTTPS 新版验证。

未做机身安装、打印 / 螺纹试配、漏光、承重或公开 HTTPS 新版本验证。

## v0.2.0 — 本地增量，2026-09-15

未提交 / 推送 / 发布本轮代码，公开站点仍为下面记录的 v0.1.0。

- 已实现可关闭的凸板 / 凹板，中空圆角矩形 / 圆形基座、法向内壁 / Z 向端面厚、80° 斜壁与 90° 直壁、有符号前端面间距，随后按实际端面材料开孔 / M 螺纹；保留外板安装结构与附属体。
- 初始演示尺寸 60×60 / R8 或 d60，壁 2、端面 2.5、80°、+17 mm；正间距须大于端面厚。原板内膜先清除，−0.1 / −1 mm 浅凹不会留下额外底层。圆柱外壳旋转 2° 参数接缝以避免共缝误分类（圆形几何不变）。
- 新增 ALPA、Arca 141、CAMBO TWR54、TOYO 158，十模板注册原字节 SHA-256 与当地前后面，浏览器逐个初始化。仍全为 experimental；STL 模板导入未实现，未据文件名宣称实机兼容。
- 默认深蓝 / 青色暗色，保留手动亮色；浅色低金属材质与方向光。真实 B-rep 拓扑边默认显示，能关闭，不影响实体 / JSON；未来 STL 来源默认关闭。相机范围及居中考虑 Z。
- schemaVersion 2，严格校验完整 schema 1 后迁移到关闭的凸凹板；无效数据不覆盖设计，应用 / 包 / 导出均 0.2.0。README / PRD / 模板目录 / CHANGELOG 已更新。
- 本轮 20 项单元测试与严格 TypeScript / 生产子路径构建通过；最终生产站点模拟 /LensboardGenerator/ 下五项 Edge / Playwright 用例全部通过（41.6 s），包含十模板、凸凹控制、凹板 STEP ZIP、端面内腔拒绝、旧 JSON 迁移、主题 / 语言 / 边线不重算 CAD及手机布局。开发站点五项亦已运行通过。
- 40 组 CAD 导出与 native OCCT / trimesh 独立重读全部通过：十模板默认孔、原有十四组加工 / 螺纹回归、新增十六组凸凹（八个主组合、凸凹 M40×1、两浅凹、R0 / R2 凸凹）。新增凸凹主要基于 Sinar；未穷举每模板全部特征组合。90° 理论体积、2.5 mm 端面和符号高度断言通过；native 材料分类检查内腔、内向法向壁、圆角偏置 / 消失、端面与外板保留，STEP 有效 / 实体数 / 体积 / 包围盒及 STL 闭合 / 法向 / 分件 / 尺寸均通过。证据在忽略的 tmp/cad-check/ 与 test-results/，README 命令可重建。
- 原 STEP 十文件身份一致，生产 dist 仅十 STEP，不含 scadtest / SLDPRT / 私有 STL。新版本尚未跑远端 Linux CI 或公开 HTTPS 验证，旧版公开结果仅见下面历史记录，不将本地子路径测试写成已上线。

未做机身 / 快门空间干涉检测、凹板遮光验证、支撑结构自动生成或实物打印试配；小模板需减小基座及孔径，保守区和当地材料检查仍可能拒绝可行设计。边线仅为可见拓扑边，不是制造剖面或隐藏线图。60 秒限时 / 单线程约 23 MB WASM 等既有限制仍适用。

## v0.1.0 — 历史公开发布与验证

日期：2026-09-14（UTC） · 实验性 alpha · 已推送并部署真实 GitHub Pages；未做实物验收。[部署证据](DEPLOYMENT.md)。

## 已实现

纯静态 React / TypeScript / Vite 网页，Three.js 预览，Replicad 1.1.0 / replicad-opencascadejs 1.1.0 单线程 WASM Worker。六个 STEP 直接内置白名单导入并核对 SHA-256；当地材料探针检查加工轮廓，Horseman 完整版保留两个实体。运行时坐标变换、厚度与保守圆区见 [模板目录](TEMPLATE_CATALOG.md)。

普通中央通孔、快门预设、自定义及偏心；可选打印直径正公差（默认关闭 / 存储 +0.5 mm）；M 基本 60°截形的真实内螺纹及明确的攻丝底孔模式；独立通孔、平底盲孔、锥形沉头、圆柱沉孔、正反面、圆周阵列、减薄座；两级法兰和扳手槽。

首次中文 / 英文切换、3D 视图、2D 孔位图点选中央孔、精确数字输入、撤销 / 重做、本地保存、受校验 JSON、分档 STL、STEP/STL ZIP。ZIP 包含参数 / 原 STEP / GPL / 来源。异常、过期或未经确认的实体不能导出；60 秒超时可重试重建内核。异步 revision 使用完整参数 JSON，不依赖文件名短 hash。

已增加亮色 / 暗色主题，首次跟随系统并记忆手动选择；主题与语言均不触发 CAD 重建。顶栏 Report 安全新标签页打开 RayforLoy/LensboardGenerator 的 Issues，不自动上传项目或创建 Issue。已提供锁文件、CI 检查和 main 推送自动 / 手动 Pages 工作流；远端为已有公开仓库。

Pages 的 build_type 为 workflow，HTTPS 已开启，仓库主页为 https://rayforloy.github.io/LensboardGenerator/。原始 STEP 以 .gitattributes 禁止 Git 换行转换，保持版本库、不同操作系统及发布资产的字节校验一致。

## 已执行的证据

| 检查 | 结果 / 范围 |
| --- | --- |
| pnpm test | 12 项通过：补偿、PCD、沉头公式、M 牙型比例、参数 / 版本、语言键一致、STL 退化三角与开放网格拒绝 |
| pnpm audit --prod | 检查时未发现已知运行依赖漏洞；不等于全面安全审计 |
| pnpm build | TypeScript 严格检查与生产构建通过；相对资源路径及 /LensboardGenerator/ 子路径构建均已运行 |
| Edge / Playwright 开发站点 | 3 项通过：真实 CAD / 打印补偿 / 双语记忆 / STEP ZIP；越界和过期导出 / 撤销 / 真实螺纹；六模板 / STL ZIP / 无效 JSON 保留设计 / 390 px 窄屏无横向溢出 |
| 本轮 UI 检查 | 4 项 Edge 用例通过（原三项及新主题 / Report 用例）；12 项单元测试及严格生产构建通过。主题跟随系统、刷新记忆、双语说明、切换无 CAD 重建、暗色警告及手机无横向溢出已检查 |
| 真实 GitHub Pages | 四项完整浏览器用例全部通过，直接访问公开 HTTPS 而非 localhost；六模板加载、实际 Worker / WASM、主题记忆、双语、STEP / STL ZIP、JSON 保护及窄屏均通过；公开下载的 STEP 另经 native OCCT 重读有效、扩孔体积符合 65.5 mm，并确认 ZIP 内原始模板字节及源码提交入口 |
| GitHub Linux CI | 09acae6 的 Checks 全部成功：12 项单元测试、严格类型 / 构建、20 组 CAD 导出与独立重读、四项 Chromium 浏览器检查；Pages 构建与部署亦成功，链接见部署记录 |
| Edge / 生产子路径 | 三项浏览器用例通过：Worker、WASM、六模板、STEP / STL ZIP 与双语均从 /LensboardGenerator/ 加载（本地模拟，非远端 Pages） |
| CAD 导出回归 | 20 组通过：六模板默认孔、两种打印孔、背面三类加工孔、沉头阵列、减薄座、短 / 长 / 左右旋法兰螺纹、板上螺纹、M62×0.75、偏心 M20×1 的 1 圈 / 20 圈 |
| 独立重读 | native OCCT 7.8.1.1 检查 20 个 STEP：有效性、实体数、体积误差 ≤0.1%、包围盒误差 ≤0.01 mm；trimesh 4.8.1 检查 20 个 STL 闭合、法向、分件、体积与包围盒 ≤0.05 mm；中间牙深的周向材料采样证明牙型保留，左右旋镜像检查通过 |

生成证据位于被忽略的 tmp/cad-check/ 与 test-results/，运行 README 命令可重建；没有把生成模型混入源模板。六个原 STEP 的 SHA-256 全部未变；六个非 STEP 原文件均被 Git 忽略，dist/assets 中恰有六个 STEP，无私有 STL / SLDPRT / 本地路径。20 圈几何 / 重读通过，不代表已完成 30 秒导出性能目标或峰值内存验收。

检查中发现并修正两个实际问题：Horseman 附属体 STL 的一个浮点退化三角（只移除零面积面，并拒绝其他非流形输出）；OCCT 多圈螺纹刀具跨圆柱接缝会误分类（同一牙型刀具分为两个角向半体依次切除）。有体积切除下限检测，不以光孔冒充真实螺纹。通过样例不是任意输入皆成功的保证。

构建提示仍有 CAD 包受 Node 环境分支影响的 node:module externalization，以及约 805 kB 主 JS chunk 的体积警告；已通过实际浏览器初始化，不隐藏警告。单线程 WASM 约 22.98 MB（gzip 7.27 MB），首次加载与内存峰值优化尚待专项检查。

## 尚未完成 / 不宣称 PRD 全部验收

- 相机装机、卡口尺寸 / 光轴偏置实测、遮光、承重及打印螺纹试配；所有模板保持 experimental，不宣称 fitTested 或 6H 制造公差等级。
- 配对镜头板 / 法兰装配、专用试配件生成、背面螺纹入口选择（当前从当地正面向内定义长度）。
- 圆柱沉孔目前为通底孔，未提供盲底孔复合选项；自由多个 M 螺纹孔暂未实现，M 内螺纹限中央孔。
- 选中孔的 3D 高亮、真实模板外轮廓 / 剖面尺寸叠加、键盘点选二维孔位及逐字段错误关联尚不完整。二维图是孔位 / 加工区示意，不是制造图纸。
- 保守圆加工区 + 实体材料探针不是完整卡口保护实体建模；可能拒绝本可加工的筋位 / 大孔。不自动修改原模板、缩孔或忽略错误。
- 任意螺距 / 极限牙数 / 多特征排列的覆盖、强制模拟 WASM 下载错误 / 导出竞态 / 内存压力、浏览器 Firefox / Safari 及离线支持尚未验收。
- 已完成真实 Pages 部署、公开浏览器验证及 Linux CI；首次独立网格检查缺少可选 graph engine，显式锁定 networkx=3.4.2 后本地与 Linux 均重验通过。正式分发方仍需复核模板首选修改形式与对应源交付方式。

P1 / P2（用户模板上传、矩形阵列、刻字、外螺纹、DXF、3MF 等）仍按 PRD 延后。第一版可用于本地设计与导出试验，不是经过制造认证的完整 P0 发布版。

## 仍需用户提供的信息

实际镜头 / 快门配合螺纹 D 与 P、相机实测基准与装机反馈、FDM / 树脂 / CNC 及材料和期望配合、镜头重量和支撑。GitHub 仓库与静态发布配置已确认。普通孔 +0.5 mm 是可调整的默认经验补偿，不承诺对所有打印机都合适。
